# Visual Test Game

A minimal game used to verify rendering and input in the bootloader runtime.

Build:
- Run `pio run` in this folder
- Copy .pio/build/pico/firmware.bin to SD root (for example as visual.bin)
